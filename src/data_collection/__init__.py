# src/data_collection/__init__.py
"""
数据采集模块 (GLM 负责)

暴露接口:
    collect_articles(sources, target_date, max_per_source) -> List[Dict]
    extract_signals(articles, llm_config) -> List[Dict]
"""

# TODO: GLM 实现以下函数
# def collect_articles(sources, target_date, max_per_source=50):
#     ...
#
# def extract_signals(articles, llm_config):
#     ...
